{
  "name": "gitlab-ci-usage",
  "version": "1.0.0",
  "description": "Lightweight CLI tool that calculates total GitLab CI job runtime across all projects in a group",
  "type": "module",
  "bin": {
    "gitlab-ci-usage": "index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node index.js",
    "test": "vitest run"
  },
  "dependencies": {
    "chalk": "^5.6.2",
    "commander": "^14.0.3",
    "date-fns": "^4.1.0",
    "ora": "^9.3.0",
    "p-limit": "^7.3.0"
  },
  "devDependencies": {
    "vitest": "^4.0.18"
  }
}
